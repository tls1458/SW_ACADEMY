import java.util.Calendar;

public class JAVA_DAY02 {

	public static void main(String[] args) {
		/* 예제 01
		// JAVA 기본, SQL
		// -> 둘을 견결함
		// Compile, Execute,
		// Data Type, Variable, Method
		int count = Integer.MAX_VALUE; // Integer 클래스에 있는 변수 = 상수 
		
		System.out.println(Integer.toBinaryString(count)); // 1의 개수 31개  , int 범위 2^(-31) ~ 2^(31)
		count+=1;
		System.out.println(Integer.toBinaryString(-1)); // 1의 개수 32개
		System.out.println(Integer.toBinaryString('1')); // 아스키코드
		
		// 타입은 크기가 있다.
		// 한정적인 메모리 공간을 효율적 사용하기 위해 변수의 크기를 적절히 지정할 필요가 있다.
		// 메모리
		// 메인 메모리, 디스크
		// nano, mili ... 
		*/
		
		
		/* 예제02
		// 제어문 : if, for, while, switch
		int sum = 0, count=1;
		while(sum <= 1000000) { // 참인 동안은 계속 반복해 !
			sum+=count++;
		}
		System.out.printf("1부터 %d까지 더하면 1,000,000을 넘으면 그 때의 값은  %d입니다.", count-1, sum);
		
		if(count!=144) { // ()안은 참이나 거짓으로 판정될 수 있어야한다.
			System.out.println("a");
		}
		
		 */
				
	
	}

}
